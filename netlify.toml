
[build]
  # أمر البناء
  command = "npm run build"
  # مجلد النشر
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# إعادة توجيه جميع المسارات إلى index.html لدعم React Router (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
